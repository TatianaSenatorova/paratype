import Swiper from 'swiper';
import { Pagination, Navigation, Scrollbar } from 'swiper/modules';

export const SwiperCore = Swiper;
export const swiperModules = { Pagination, Navigation, Scrollbar };
